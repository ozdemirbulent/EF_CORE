@{
    ViewData["Title"] = "AllCountSmallerThen5";
    Layout = "~/Views/Layout/Index.cshtml";
}

<div class="row">
    <div class="col-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Stok kontrolü (All Metodu)</h4>

                <br />
                <div class="alert alert-success">@ViewBag.message</div>
            </div>
        </div>
    </div>

</div>

